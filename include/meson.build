###########
# headers #
###########

if not meson.is_subproject()

  # pacbio/pancake
  install_headers(
    files([
      'pacbio/Version.h',
    ])
    # subdir : '')
  )

endif

pancake_include_directories = include_directories('.')